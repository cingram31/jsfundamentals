// Conditionals
/*
	Chaining conditional operators
*/
var name = "Kenn"
if ( "Kenn" == name ) {
	console.log("Hey your name is " + name);
} else {
	console.log("What is your name?");
};


// Create an If statement that checks if the users name does not have a capital letter,
// Rebuild their name with the capitalized first letter

var name = "christian"
if ("Christian" == name) {
	console.log("christian is your name? " + name);
}	else {
	console.log("Christian!");
}

var fname = "kenn";

if (fname [0] != fname[0].toUpperCase()) {
(fname [0] != fname[0].toUpperCase() + fname[1] + fname[2] +fname[3];
	console.log("Horrible punction, you need to uppercase the first letter like this")
}
	else {
		console.log("Great punctuation!");
	}

	//when writing if statements order matters.
var age = 16
if (age >= 18) {
	console.log("Yay you can vote!");
}  else if (age >= 21) {
	console.log("Yay you can drink!");
} else if (age >= 25) {
	console.log("Yay you get lower rates on your car rental")
}else{
	console.log("sorry too young to do anything!")
}

//
